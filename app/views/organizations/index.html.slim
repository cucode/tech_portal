h1.page-header
  = link_to "Submit My Organization", new_organization_path, class: "pull-right btn btn-success"
  | Tech-Related Organizations
- @organizations.to_a.each_slice(3) do |slice|
  .row-fluid
    - slice.each do |organization|
      .span4.well
        = render organization

= paginate @organizations
